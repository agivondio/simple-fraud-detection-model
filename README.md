# Simple Fraud Detection Model

This project is a basic implementation of a fraud detection system using machine learning.

## Features
- Detects fraudulent transactions based on:
  - transaction amount
  - frequency
- Uses Isolation Forest algorithm

## How to Run

```bash
pip install -r requirements.txt
python model.py